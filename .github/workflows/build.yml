name: Build and Push Docker Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    strategy:
      matrix:
        php-version: ['8.1', '8.2', '8.3', '8.4']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push App image
        id: build-app
        uses: docker/build-push-action@v5
        with:
          context: ./app
          build-args: |
            PHP_VERSION=${{ matrix.php-version }}
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/app:${{ matrix.php-version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push CLI image
        id: build-cli
        uses: docker/build-push-action@v5
        with:
          context: ./cli
          build-args: |
            PHP_VERSION=${{ matrix.php-version }}
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/cli:${{ matrix.php-version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Worker image
        id: build-worker
        uses: docker/build-push-action@v5
        with:
          context: ./worker
          build-args: |
            PHP_VERSION=${{ matrix.php-version }}
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/worker:${{ matrix.php-version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  sbom:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
      attestations: write
      id-token: write
    needs: build

    strategy:
      matrix:
        image: ['app', 'cli', 'worker']
        php-version: ['8.1', '8.2', '8.3', '8.4']

    steps:
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.image }}:${{ matrix.php-version }}
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-results.sarif'
